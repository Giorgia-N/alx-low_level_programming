#include "main.h"
/**
 * main - prints the mane of the file
 * @argv: array
 * @argc: count
 * Return: Alwats 0
 */
int main(int argc_attribute_(unused), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
